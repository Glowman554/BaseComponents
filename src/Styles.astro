<style is:global>
    * {
        font-family: 'JetBrains Mono', monospace;
    }

    .field {
        @apply rounded-xl bg-slate-100 p-4;
    }

    .section {
        @apply m-4 flex justify-between;
    }

    .section-small {
        @apply flex justify-between;
    }

    .section-small:hover {
        @apply bg-slate-600;
    }

    .center {
        @apply flex justify-center;
    }

    .button {
        @apply rounded-xl bg-slate-500 p-2;
    }

    .responsive .entry {
        @apply max-sm:float-none max-sm:!mb-1 max-sm:!mt-1 max-sm:!block max-sm:text-left;
    }
</style>
